import { defineComponent } from 'vue'

const MultipleSelector = defineComponent({
  name: 'MultipleSelector',
  setup() {
    return () => <div>multiple selector</div>
  },
})

export default MultipleSelector
